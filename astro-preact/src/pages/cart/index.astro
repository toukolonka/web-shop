---
import Layout from '../../layouts/Layout.astro';
import Cart from '../../components/Cart.jsx';

if(Astro.params.formSubmitted) {
	return Astro.redirect('/orders');
}
---

<Layout>
	<Cart client:only="preact" />
</Layout>